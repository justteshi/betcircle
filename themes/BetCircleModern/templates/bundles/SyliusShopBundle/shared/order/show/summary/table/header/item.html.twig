{% set label = hookable_metadata.configuration.label %}

<th class="border-bottom text-nowrap {{ hookable_metadata.configuration.class|default('') }}" data-test-table="{{ hookable_metadata.configuration.test_id|default(label) }}">
    {{ ('sylius.ui.' ~ label)|trans }}
</th>
